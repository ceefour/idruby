<h1>Creating New rule</h1>

<% form_for(@rule) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'form', :locals => {:f => f}  %>
  <p><%= f.submit "Create" %></p>
<% end %>

<%= link_to 'Back', rules_path %>
