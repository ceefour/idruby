<h1>New publisher</h1>

<% form_for(@publisher) do |f| %>
  <%= f.error_messages %>
    <%= render :partial => 'form', :locals => {:f => f} %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', publishers_path %>
