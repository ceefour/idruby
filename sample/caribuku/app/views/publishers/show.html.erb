<% for column in Publisher.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%=h @publisher.send(column.name) %>
</p>
<% end %>
<%= link_to 'Edit', edit_publisher_path(@publisher) %> |
<%= link_to 'Back', publishers_path %>
